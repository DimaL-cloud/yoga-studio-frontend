export class Teacher {
  name: string;
  title: string;
  photo: string;

  constructor(name: string, title: string, photo: string) {
    this.name = name;
    this.title = title;
    this.photo = photo;
  }
}
